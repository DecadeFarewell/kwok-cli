{
  "presets": [
    [
      "@babel/preset-env",
      {
        "targets": {
          "node": "current" // 目标为当前 Node.js 版本
        },
        "modules": "commonjs" // 输出 CommonJS 模块
      }
    ],
    "@babel/preset-typescript" // ts 编译成 js
  ],
  "sourceMaps": "both" // 生成sourceMap
}
